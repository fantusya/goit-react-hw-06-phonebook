export const getContacts = state => state.contactsAll.contacts;

export const getFilter = state => state.filter;

// export const getContacts = state => state.contacts.items;

// export const getFilter = state => state.contacts.filter;
